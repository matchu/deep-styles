{
  "name": "deep-styles",
  "version": "0.0.1",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm run compile-js && npm run compile-flow",
    "compile-js": "babel src/ -d build/",
    "compile-flow": "cp src/exported-types.js build/exported-types.js && cp src/apply/css/exported-types.js build/apply/css/exported-types.js && cp src/index.js.flow build/index.js.flow",
    "watch": "babel --watch src/ -d build/",
    "flow": "flow",
    "prepublish": "npm run flow && npm run compile"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-0": "^6.24.1",
    "flow-bin": "^0.46.0"
  },
  "dependencies": {
    "string-hash": "^1.1.3"
  }
}
